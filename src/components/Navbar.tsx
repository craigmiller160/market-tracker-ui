import {Menu} from 'antd';
import './Navbar.scss';

export const Navbar = () => {
    return (
        <Menu className="Navbar">
            <Menu.Item key="first">First</Menu.Item>
        </Menu>
    )
};