export {};

describe('Search', () => {
	it('renders initial layout correctly', () => {
		throw new Error();
	});

	it('enables/disables search button based on symbol content', () => {
		throw new Error();
	});
});
