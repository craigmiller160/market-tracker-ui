import { useInterval } from '../../../src/components/utils/useInterval';

describe('useInterval', () => {
	let count = 0;
	beforeEach(() => {
		count = 0;
	});
	// TODO delete this and the useInterval file if not used

	it('runs action at interval', async () => {
		throw new Error();
	});

	it('changes action while running at interval', async () => {
		throw new Error();
	});

	it('changes interval rate', async () => {
		throw new Error();
	});
});
