export {};

// TODO delete this in the end

describe('mirage server', () => {
	it('gets data with request', () => {
		throw new Error();
	});

	it('modifies mirage data with request', () => {
		throw new Error();
	});
});
