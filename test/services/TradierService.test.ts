export {};

describe('TradierService', () => {
	it('get single quote', () => {
		throw new Error();
	});

	it('get multiple quotes', () => {
		throw new Error();
	});

	it('get 1 week history', () => {
		throw new Error();
	});

	it('get 1 month history', () => {
		throw new Error();
	});

	it('get 3 months history', () => {
		throw new Error();
	});

	it('get 1 year history', () => {
		throw new Error();
	});

	it('get 5 years history', () => {
		throw new Error();
	});
});
