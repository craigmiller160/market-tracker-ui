export {};

describe('auth actions', () => {
	describe('loadAuthUser', () => {
		it('user is authenticated', () => {
			throw new Error();
		});

		it('user is not authenticated', () => {
			throw new Error();
		});
	});
});
