export {};

describe('selectors', () => {
	describe('isAuthorizedSelector', () => {
		it('is authorized', () => {
			throw new Error();
		});

		it('is not authorized', () => {
			throw new Error();
		});
	});
});
