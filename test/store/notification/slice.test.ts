export {};

describe('notification slice', () => {
	it('addSuccess', () => {
		throw new Error();
	});

	it('addError', () => {
		throw new Error();
	});

	describe('markShown', () => {
		it('finds match', () => {
			throw new Error();
		});

		it('cannot find match', () => {
			throw new Error();
		});
	});

	describe('hide', () => {
		it('finds match', () => {
			throw new Error();
		});

		it('cannot find match', () => {
			throw new Error();
		});
	});
});
